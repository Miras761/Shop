# 🛍️ МаркетПлейс — Django + DRF + Vanilla JS

Полноценный интернет-магазин / доска объявлений в стиле OLX/Авито.

## ✨ Функциональность

- 📋 Объявления с фото, ценой, описанием, категорией
- 🔍 Поиск, фильтрация по цене, состоянию, категории
- ❤️ Избранное
- 💬 Сообщения продавцу
- 👤 Профиль пользователя
- 🔐 Регистрация и авторизация (токены)
- 📱 Адаптивный дизайн (мобильный + десктоп)
- 🔧 Django Admin для управления

---

## 🚀 Деплой на Render (бесплатно, 5 минут)

### Шаг 1: Загрузите код на GitHub

1. Создайте аккаунт на [github.com](https://github.com)
2. Создайте новый репозиторий (New repository)
3. Загрузите все файлы проекта:

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/ВАШ_ЛОГИН/marketplace.git
git push -u origin main
```

### Шаг 2: Создайте сервис на Render

1. Зарегистрируйтесь на [render.com](https://render.com)
2. Нажмите **New → Web Service**
3. Подключите GitHub и выберите ваш репозиторий
4. Render автоматически обнаружит `render.yaml` и всё настроит

**Или вручную:**
- **Name:** marketplace
- **Runtime:** Python 3
- **Build Command:** `./build.sh`
- **Start Command:** `gunicorn marketplace.wsgi:application --bind 0.0.0.0:$PORT`

### Шаг 3: Добавьте PostgreSQL (рекомендуется)

1. В Render → **New → PostgreSQL**
2. Создайте БД, скопируйте **Internal Database URL**
3. В настройках Web Service → Environment Variables:
   - `DATABASE_URL` = (вставьте Internal Database URL)
   - `SECRET_KEY` = (сгенерируйте: `python -c "import secrets; print(secrets.token_urlsafe(50))"`)
   - `DEBUG` = `false`

### Шаг 4: Deploy!

Нажмите **Manual Deploy → Deploy latest commit**

Ваш сайт будет доступен по адресу типа: `https://marketplace-xxxx.onrender.com`

---

## 💻 Локальный запуск

```bash
# 1. Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate

# 2. Установите зависимости
pip install -r requirements.txt

# 3. Скопируйте .env
cp .env.example .env
# Отредактируйте .env при необходимости

# 4. Выполните миграции
python manage.py migrate

# 5. Заполните категории и создайте admin
python seed_data.py
# Создаётся пользователь admin / admin123

# 6. Запустите сервер
python manage.py runserver
```

Откройте http://127.0.0.1:8000

**Админ-панель:** http://127.0.0.1:8000/admin/  
Логин: `admin`, Пароль: `admin123`

---

## 🏗️ Структура проекта

```
marketplace/
├── marketplace/          # Django проект (настройки, URL)
│   ├── settings.py
│   ├── urls.py
│   └── wsgi.py
├── apps/
│   ├── users/            # Авторизация, профили
│   ├── categories/       # Категории объявлений
│   └── listings/         # Объявления, избранное, сообщения
├── templates/
│   └── index.html        # SPA фронтенд (Vanilla JS)
├── static/               # Статические файлы
├── requirements.txt
├── render.yaml           # Конфиг для Render
├── build.sh              # Скрипт сборки
└── seed_data.py          # Тестовые данные
```

## 🔌 API Endpoints

| Метод | URL | Описание |
|-------|-----|----------|
| GET | /api/listings/ | Список объявлений (поиск, фильтры) |
| POST | /api/listings/create/ | Создать объявление |
| GET | /api/listings/{id}/ | Объявление (+ +1 просмотр) |
| POST | /api/listings/{id}/favorite/ | Добавить/убрать из избранного |
| GET | /api/categories/ | Категории |
| POST | /api/auth/register/ | Регистрация |
| POST | /api/auth/login/ | Вход |
| GET | /api/auth/profile/ | Профиль |
| GET | /api/my/listings/ | Мои объявления |
| GET | /api/my/favorites/ | Избранное |
| GET | /api/my/messages/ | Входящие сообщения |
| POST | /api/messages/ | Отправить сообщение |

---

## 🎨 Технологии

- **Backend:** Django 4.2, Django REST Framework, WhiteNoise, Gunicorn
- **Database:** SQLite (dev) / PostgreSQL (prod)
- **Frontend:** Vanilla JS, CSS3 (без зависимостей!)
- **Deploy:** Render.com
